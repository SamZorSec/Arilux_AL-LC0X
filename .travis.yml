language: c
sudo: false
before_install:
  - source <(curl -SLs https://raw.githubusercontent.com/adafruit/travis-ci-arduino/master/install.sh)
install:
  - arduino --install-library "PubSubClient,IRremoteESP8266,RCSwitch,ArduinoJson"
script:
  - build_main_platforms esp8266
